const db=require('./../../config/connection2');


module.exports={
    doSignUp:(res)=>{
        console.log("responses are",res);
    //     db.bookdata.find()
    // .then((books)=>{

    //     res.render("books",{data,books});
    // })
    }
}